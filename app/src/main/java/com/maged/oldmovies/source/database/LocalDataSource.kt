package com.maged.oldmovies.source.database

/**
 * Organizational class that has access to all local data sources : DB & whatever else in the filesystem
 */
class LocalDataSource(val database: AppDatabase) {
}